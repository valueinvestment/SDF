---
"@sdf/plugin-runtime": minor
"@sdf/types": patch
---

Finalize PluginProps as a useStoreSlice selector hook (Render-Bypass for 10Hz store updates), thread it through PluginPanel.component, and ship two example plugins.
