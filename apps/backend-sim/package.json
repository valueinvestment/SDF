{
  "name": "@sdf/backend-sim",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "uv run uvicorn main:app --reload --host 0.0.0.0 --port 8000",
    "test": "uv run pytest",
    "lint": "echo 'no JS lint for Python backend'"
  }
}
